
sudo passwd root

sudo raspi-config
Advanced Options->Memory Split

sudo dpkg-reconfigure tzdata

sudo vi /etc/dhcpcd.conf
interface eth0
static ip_address=192.168.1.21/24
static routers=192.168.1.1
static domain_name_servers=192.168.1.1 8.8.8.8

wget https://raw.githubusercontent.com/tshr20140816/files/master/memo/20180422.sh
sudo bash 20180422.sh

sudo adduser xxx

sudo visudo
xxx ALL=(ALL) NOPASSWD: ALL

sudo vi /etc/systemd/timesyncd.conf
NTP=ntp.jst.mfeed.ad.jp

sudo systemctl restart systemd-timesyncd
sudo dpkg-reconfigure tzdata

sudo hostnamectl set-hostname raspberrypizero
sudo vi /etc/hosts

sudo reboot

sudo userdel -r pi

sudo apt-get -y --purge remove aptitude aptitude-common isc-dhcp-client isc-dhcp-common wireless-regdb wireless-tools wpasupplicant bluez bluez-firmware pi-bluetooth

sudo apt-get -y autoremove
sudo apt-get -y autoclean

sudo apt-get clean
sudo apt-get update
sudo apt-get -y upgrade
sudo apt-get dist-upgrade

sudo vcgencmd version

sudo apt-get install telnetd vim

sudo systemctl disable ssh.service

sudo reboot

sudo apt-get install postfix bsd-mailx sasl2-bin libsasl2-modules
sudo cp /usr/share/postfix/main.cf.dist /etc/postfix/main.cf

sudo vi /etc/postfix/main.cf
変更
setgid_group = postdrop

追記
relayhost = [smtp.gmail.com]:587
smtp_use_tls = yes
smtp_tls_CApath = /etc/pki/tls/certs/ca-bundle.crt
smtp_sasl_auth_enable = yes
smtp_sasl_password_maps = hash:/etc/postfix/sasl_passwd
smtp_sasl_tls_security_options = noanonymous
smtp_sasl_mechanism_filter = plain

sudo vi /etc/postfix/sasl_passwd
[smtp.gmail.com]:587 xxx@gmail.com:password

sudo chmod 600 /etc/postfix/sasl_passwd
sudo postmap /etc/postfix/sasl_passwd

sudo vi /etc/aliases
root: user

sudo newaliases
